+------------------------------------------------------------------ 
                        Integrantes do grupo
+------------------------------------------------------------------

Amanda Maria Fabio Tavares 01
Bianca Oliveira de Camargo 03
Carla Julia Franco de Toledo 04
Felipe Lima Estevanatto 06
Gabriel Gomes Nicolim 08

Link para acesso direto:

http://200.145.153.175/felipeestevanatto/trab2bimPHP/ALICE/
+------------------------------------------------------------------ 
                           Criar tabelas
+------------------------------------------------------------------
CREATE TABLE usuarios (
id_user SERIAL PRIMARY KEY NOT NULL,
nome VARCHAR(40) NOT NULL,
email VARCHAR(128) NOT NULL,
senha CHAR(32) NOT NULL
);

CREATE TABLE registros (
idRegistro BIGSERIAL PRIMARY KEY NOT NULL,
nomeProd VARCHAR(100),
qntProd INT NOT NULL,
tipoProd VARCHAR(20),
valorProd DECIMAL(10,2),
excluido BOOLEAN NOT NULL,
data_exclusao DATE,
fk_user INT NOT NULL,
FOREIGN KEY (fk_user) REFERENCES usuarios (id_user)
);

+------------------------------------------------------------------

  Dropar tabelas |(Enquanto a exclusão em massa não está pronta)|

+------------------------------------------------------------------

DROP TABLE registros; DROP TABLE usuarios;